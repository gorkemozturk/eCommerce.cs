@model IEnumerable<eCommerce.cs.Data.Entities.ProductType>
@{
    ViewData["Title"] = "Product Types";
}

<div class="panel panel-default">
    <div class="panel-heading text-center">
        PRODUCT TYPES
    </div>
    <div class="panel-body text-right">
        <a asp-action="Create" class="btn btn-primary btn-sm">Create a New Product Type</a>
    </div>
    <hr style="margin: 0" />
    <div style="padding: 0" class="panel-body">
        <table style="margin: 0" class="table table-striped">
            <thead>
                <tr>
                    <th class="col-md-4">@Html.DisplayNameFor(model => model.ProductID)</th>
                    <th class="col-md-4">@Html.DisplayNameFor(model => model.ProductName)</th>
                    <th class="col-md-4 text-center">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="col-md-4">
                            @Html.DisplayFor(modelItem => item.ProductID)
                        </td>
                        <td class="col-md-4">
                            @Html.DisplayFor(modelItem => item.ProductName)
                        </td>
                        <td class="col-md-4 text-center">
                            <partial name="_TableButtonsPartial" model="item.ProductID" />
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>